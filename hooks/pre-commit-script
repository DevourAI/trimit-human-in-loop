#!/bin/sh

# Run the make format command
make format

# Check the exit status of the make command
if [ $? -ne 0 ]; then
  echo "The make format command failed. Aborting commit."
  exit 1
fi

git add .

# Exit with a zero status if everything is fine
exit 0
